import React from 'react'

const Error = ({message}) => {
    return (
        <div style={{ width: "-webkit-fill-available", textAlign: "center", fontSize: "xxx-large" }}>{message}</div>
    )
}

export default Error